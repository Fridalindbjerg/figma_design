<header class="full-bleed">
  <div class="wrapperHeader container">
    <p>AskExperts</p>
    <ul>
      <li>
        <a href="/"> Home</a>
      </li>
      <li>
        <a href="/case">Case Study</a>
      </li>
      <li>
        <a href="/team">Team</a>
      </li>
      <li>
        <a href="/about">About</a>
      </li>
    </ul>

    <button popovertarget="popup"> Login </button>
  </div>

  <!-- <a href="#" popovertarget="popup">Login</a> -->

  <div id="popup" popover>
    <div class="login-box">
      <h2>Dashboard Login</h2>
      <form>
        <div>
          <label for="email">Email address</label>
          <input type="email" id="email" name="email" />
        </div>

        <div>
          <label for="password">Password</label>
          <input type="password" id="password" name="password" />
        </div>

        <a type="submit">Go to dashboard</a>
      </form>
    </div>
  </div>
</header>

<style>
  header {
    background-color: var(--bg-header);
  }
  .wrapperHeader {
    display: flex;
    justify-content: space-between;
    align-items: center;
    color: var(--white);
    /* font-family: Cabin; */

    p {
      font-size: var(--step--4);
    }

    ul {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 30px;

      li {
        list-style: none;
        a {
          color: white;
          text-decoration: none;
          font-size: var(--step--5);
        }
      }

      button {
        text-decoration: none;
        list-style: none;
        color: var(--white);
      }
    }

    ul + button {
      border-radius: 20px;
      background: var(--yellow);
      padding: 4px 16px;
      color: black;
    }
    /* .full-width {
      grid-column: 1 / -1;
    } */
  }

  [popover] {
    border: none;
    background: transparent;
    padding: 0;
    opacity: 0;
    translate: 0 -100%;
    transition: 0.6s;
    transition-property: opacity, display, overlay, translate;
    transition-behavior: allow-discrete;
  }

  [popover]:popover-open {
    opacity: 1;
    translate: 0 0;
  }

  [popover]::backdrop {
    background: rgba(0, 0, 0, 0.4);
    backdrop-filter: blur(8px); /* blurer alt bag overlay */
    -webkit-backdrop-filter: blur(8px);
  }

  .login-box {
    background-color: #181818;
    padding: 2rem;
    border-radius: 1rem;
    width: 100%;
    max-width: 360px;

    h2 {
      font-weight: 500;
      margin-bottom: var(--space-l);
      color: white;
      font-size: var(--step--2);
    }

    form {
      display: flex;
      flex-direction: column;
      gap: var(--space-m);
    }

    label {
      font-size: var(--step--4);
      color: white;
    }

    input {
      background-color: white;
      width: 100%;
      padding: 0.9375rem 3.5rem;
      border-radius: 6px;
      border: none;
      font-size: var(--step--4);
    }

    input:focus {
      outline: 2px solid var(--yellow);
    }

    form > a {
      margin-top: 1rem;
      background-color: var(--yellow);
      color: #000;
      border-radius: 9999px;
      padding: 0.7rem 1rem;
      font-size: 1rem;
      font-weight: 400;
      cursor: pointer;
      transition: background-color 0.2s ease;
      align-self: center;
    }

    form > a:hover {
      background-color: #f6c145;
    }
  }
</style>
