---
import { Image } from "astro:assets";
import Services from "./Services.astro";
import CEOImage from "@assets/images/ceo.png";
---

<section class="hero">
  <div class="hero-content">
    <div>
      <h1>Empowering Your Business Growth</h1>
      <p>Discover tailored strategies and expert insights to take your business to the next level. Let us help you navigate challenges and achieve sustainable success.</p>
      <a href="#0">Book a Meeting</a>
    </div>
    <Image src={CEOImage} alt="CEO" />
  </div>
  <Services />
</section>

<style>
  /* Styles her */
  @layer components {
    /* .hero {
      display: grid;
      grid-template-columns: subgrid;
      grid-column: full-start / full-end;
      background-color: #f3f4f6;
      padding: 2rem;
    }

    .hero-content {
      display: grid;
      grid-template-columns: subgrid;
    }

    .hero-content img {
      grid-column: 3 / full-end; 
      width: 100%;
      height: auto;
      object-fit: cover;
    } */

    .hero {
      display: grid;
      grid-template-columns: subgrid; /* kun virker hvis parent-grid har definerede kolonner */
      grid-column: full-start / full-end; /* korrekt — går full-bleed */
      background-color: #f3f4f6;
      padding: 2rem;
    }
    .hero-content {
      display: grid;
      grid-template-columns: subgrid;
    }
  }
</style>
